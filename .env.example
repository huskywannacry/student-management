# Copy this file to .env and adjust values for local development

# Used for local dev (outside Docker) – host is localhost
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/website_management

# Used by docker-compose to build the DATABASE_URL inside containers (host = postgres service)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=
POSTGRES_DB=website_management